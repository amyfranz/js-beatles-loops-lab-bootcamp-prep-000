function theBeatlesPlay(musicians, instruments){
  var newArray = [];
  newArray.length = musicians.length;
  for (var i = 1; i < Array.length(musicians); i++) {
    newArray[i] = `${musicians[i]} plays ${
  }
}